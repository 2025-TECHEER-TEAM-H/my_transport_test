# FastAPI: 현대적인 Python 웹 프레임워크 (Swagger UI 자동 제공)
# RESTful API를 쉽게 구축할 수 있고, 자동 API 문서화 기능 제공
fastapi==0.104.1

# uvicorn: ASGI 서버 (FastAPI를 실행하기 위한 서버)
# standard 옵션: 성능 향상을 위한 추가 의존성 포함
uvicorn[standard]==0.24.0

# httpx: 비동기 HTTP 클라이언트 라이브러리
# 외부 API와 통신하기 위해 사용 (requests의 비동기 버전)
httpx==0.25.2

# python-dotenv: .env 파일에서 환경 변수를 읽어오는 라이브러리
# API 키 등 민감한 정보를 코드에서 분리하여 관리
python-dotenv==1.0.0

# pydantic: 데이터 검증 라이브러리 (FastAPI가 내부적으로 사용)
# FastAPI와 함께 설치되어 API 요청/응답 데이터 검증에 사용
pydantic==2.5.0
